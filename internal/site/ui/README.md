# App UI Source Copies

This tree mirrors the reusable source archives:

- `blocks/storefront` mirrors `Blocks/storefront`
- `elements/commerce` mirrors `Elements/commerce`
- `elements/selection` mirrors `Elements/selection`
- `elements/toggles` mirrors `Elements/toggles`
- `elements/navigation` mirrors `Elements/navigation`

Keep source files here and regenerate `*_templ.go` with `templ generate ./...`.
